/* Specifications describe the fitted artifact, including approximation limits. */
globalThis.salaryModelVignette = (row, model, artifact) => {
  const escape = (value) => String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll('"', "&quot;");
  const bayesian = row.method.startsWith("bayesian");
  const smooth = row.method === "bayesianGam";
  const numeric = row.method !== "intercept";
  const keys = (model.preprocessing || []).map((item) => item.key.replaceAll("_", " "));
  const section = (title, body) => `<section><h3>${title}</h3>${body}</section>`;
  const equation = (text) => `<pre class="model-equation">${escape(text)}</pre>`;
  let html = `<p><strong>Target:</strong> annual CEO base salary in July 2026 dollars. The response y = log(salary) uses natural logarithms. RP pay is excluded from fitting.</p>`;
  html += section("Data and predictors", `<p>${bayesian ? `This variant uses ${row.exactN} exact base-pay filings and ${row.cashProxyN} cash-only filings${row.includeAdvertisedRanges ? `, plus ${row.advertisedRangeN} advertised ranges and ${row.advertisedPointN} advertised points` : ""}.` : `This model uses the same ${row.exactN} exact base-pay filings as the other numeric comparison models.`} ${numeric ? `Numeric inputs are ${escape(keys.join(", "))}. Each positive input is logged and standardized using the observed training mean and standard deviation. Missingness indicators are also included.` : "No organization predictors enter this model."} ${bayesian ? "Focus, organization type, title, CEO hiring market, work arrangement, fiscal sponsorship, and EA relationship add categorical effects." : "Categorical profile settings, cash proxies, and job ads do not enter this model."}</p><p>Every preprocessing estimate is learned inside its training fold. The browser requires positive values for all active numeric profile inputs. The workforce measure is the filing’s employee count, which may omit contractors and overseas affiliates. Highest-other pay is selected after converting eligible disclosed employees to a 40-hour week: combined annual base pay × 40 / combined reported weekly hours. Reported effort is not contractual FTE. Related-organization pay and hours are combined, even when one entity pays the entire salary. Unresolved hours and partial-year/former roles are excluded. Missing base disclosures can still conceal a higher salary.</p>`);
  if (bayesian) {
    html += section("Salary model", equation(`yᵢ | θ, xᵢ ∼ Normal(μᵢ, σfiling)
μᵢ = α + Σⱼ βⱼ zᵢⱼ + Σⱼ βmissing,ⱼ mᵢⱼ${smooth ? " + Σⱼ Σₖ θⱼₖ Bⱼₖ(zᵢⱼ)" : ""}
     + Σg ug,category(i) + δEA · I(EA-adjacent)`) + `<p>The linear and categorical effects are signed. Categories have partial pooling: a sparsely represented category is pulled toward its group mean. Each group’s effects sum to zero across its fixed category taxonomy. EA’s reference is Functional overlap, with one signed EA-adjacent contrast.</p>`);
    if (smooth) html += section("Regularized curvature", equation(`dⱼ(x) = [(x − κⱼ)₊³ − (x − κ₄)₊³] / (κ₄ − κⱼ)
raw Bₖ(x) = dₖ(x) − d₃(x),  k ∈ {1, 2}
Bₖ(x) = [raw Bₖ(x) − aₖ − bₖx] / sₖ
θⱼₖ = τsmooth,ⱼ ηⱼₖ;  ηⱼₖ ∼ Normal(0, 1)
τsmooth,ⱼ ∼ half-Normal(0, 0.15)`) + `<p>Each numeric input has four knots at its training 10th, 35th, 65th, and 90th percentiles. Least-squares projection removes the constant and linear components from each curvature basis; s is its training residual standard deviation. The two curvature terms supplement the ordinary slope and have linear tails beyond the boundary knots. Shrinking τ toward zero recovers a straight line. The basis is evaluated at latent missing inputs inside Stan, preserving uncertainty rather than fixing an imputed spline value.</p>`);
    html += section("Priors and missing inputs", equation(`α ∼ Normal(12.5, 1);  β ∼ Normal(0, 0.3)
ug,j = τg (ag,j − mean(ag));  ag,j ∼ Normal(0, 1)
τg ∼ half-Normal(0, 0.25), or 0.20 for remote/fiscal sponsor
δEA ∼ Normal(0, 0.20)
σfiling ∼ Normal(0.35, 0.15) restricted to [0.12, 1.5]
σad ∼ Normal(0.45, 0.18) restricted to [0.12, 1.5]
zᵢ ∼ MVN(ξ, diag(s) Ω diag(s))
ξⱼ ∼ Normal(0, 0.5);  sⱼ ∼ Lognormal(0, 0.35);  Ω ∼ LKJ(2)`) + `<p>Observed numeric inputs inform the joint input distribution; missing training inputs are latent parameters. Held-out missing inputs are drawn conditionally on that organization’s observed inputs from the training posterior. Held-out salaries never inform those draws. This is an observational model under an ignorable-missingness assumption conditional on included covariates and missingness indicators; it does not identify a nonrandom disclosure mechanism.</p>`);
    html += section("Different compensation disclosures", equation(`log(cash) = log(base) + D
P(D = 0) = π;  D | D > 0 ∼ Exponential(λ)
π ∼ Beta(2, 2); λ ∼ Lognormal(log(10), 0.6), λ ∈ [0.5, 100]
μad = μ + δad;  δad ∼ Normal(0, 0.35)
L(range [L, U]) = Φ((log U − μad)/σad) − Φ((log L − μad)/σad)`) + `<p>Exact base pay uses the normal likelihood. Where both base and cash are known, the increment informs π and λ. Cash-only records use the marginalized zero-increment/exponentially modified normal mixture; they are never relabeled as base pay. ${row.includeAdvertisedRanges ? "Advertised points use the ad normal density; advertised ranges use the probability mass above. Policy ranges are experimental information about intended pay, not random censoring of incumbent salaries." : "This variant excludes advertised evidence; its ad parameters therefore retain their priors."}</p>`);
  } else if (row.method === "gp") {
    html += section("Gaussian process", equation(`yᵢ = a + f(zᵢ) + εᵢ
a ∼ Normal(12.5, 1); f ∼ GP(0, k); εᵢ ∼ Normal(0, σ)
k(z, z′) = A² exp(−‖z − z′‖² / (2ℓ²))
K = 11ᵀ + A²R + σ²I
log ℓ ∼ Normal(log 1, 0.7)
log A ∼ Normal(log 0.3, 0.6); log σ ∼ Normal(log 0.3, 0.6)`) + `<p>The RBF covariance allows smooth interactions between numeric inputs and missingness indicators. The mean and latent function are integrated analytically. Kernel parameters maximize the marginal posterior density in log-parameter coordinates, with three L-BFGS-B starts (ℓ = 0.4, 1, 3). Bounds are ℓ ∈ [0.05,20], A ∈ [0.02,2], σ ∈ [0.03,1.5]. The same fitting occurs independently inside every outer fold. Missing logged inputs are filled at the training mean.</p><p><strong>Empirical Bayes:</strong> predictions integrate uncertainty in a and f, while ℓ, A, and σ are held at their fitted values. Reported credible intervals are conditional on those hyperparameters and omit uncertainty in their estimation.</p><p>Full-fit values: ℓ = ${model.kernel.lengthScale.toFixed(3)}, A = ${model.kernel.amplitude.toFixed(3)}, σ = ${model.kernel.noise.toFixed(3)}.</p>`);
  } else if (row.method === "svr") {
    html += section("Support-vector regression", equation(`minimize  ½ ‖w‖² + C Σᵢ max(0, |yᵢ − f(zᵢ)| − ε)
f(z) = b + Σᵢ wi exp(−γ ‖z − zᵢ‖²)`) + `<p>The ε-insensitive loss tolerates small errors; the norm penalty controls complexity. RBF features allow smooth nonlinear interactions. R’s e1071/libsvm uses ε-regression, radial kernel, and scale = FALSE because preprocessing is explicit. Four organization-grouped inner folds select minimum summed squared held-out log error from C ∈ {1,4,16}, γ ∈ {0.1,0.4}, ε ∈ {0.05,0.15}; ties follow that grid order. Every inner fold recomputes preprocessing. Missing inputs are filled at the training log mean. Full-fit choices: C = ${model.kernel.cost}, γ = ${model.kernel.gamma}, ε = ${model.kernel.epsilon}.</p>`);
  } else if (row.method === "gam") {
    html += section("Additive smooths", equation(`yᵢ = α + Σⱼ fⱼ(zᵢⱼ) + Σⱼ βmissing,ⱼ mᵢⱼ + εᵢ
fit by penalized least squares; smoothness chosen by REML`) + `<p>mgcv::gam uses Gaussian identity-link regression with s(z, k = 4, bs = "cr") for each numeric input. Each cubic regression spline has a small basis and an integrated squared-curvature penalty. There are no interactions. Active missingness indicators enter linearly; inputs are filled at the training log mean. Prediction grids use at least 141 points, span at least ±3.5 standardized units, and cover observed support. The browser linearly interpolates these grids and holds their endpoint effects beyond them.</p>`);
  } else {
    html += section("Mean model", equation(row.method === "intercept" ? "yᵢ = α + εᵢ;  α̂ = mean(y)" : "yᵢ = α + Σⱼ βⱼzᵢⱼ + Σⱼ βmissing,ⱼmᵢⱼ + εᵢ") + `<p>Ordinary least squares estimates the mean log salary. ${row.method === "linear" ? "Missing numeric inputs are filled at the training log mean. Constant and linearly dependent design columns are explicitly removed inside each fit; active and dropped columns are saved." : "The salary median before residual calibration is exp(α̂), the geometric mean of observed salaries."}</p>`);
  }
  if (!bayesian && row.method !== "gp") html += section("Predictive calibration", equation(`p(y | z) = (1/M) Σᵢ Normal(y | f̂(z) + ri, h)
h = max(0.04, 1.06 sd(r) M^(−1/5))`) + `<p>Residuals come from held-out organizations. Each outer test fold is excluded from the nine inner residual-calibration fits${row.method === "svr" ? ", including each inner fit’s own four-fold hyperparameter search" : ""}. Thus calibration never sees its outer test outcomes. For the full-data browser prediction, residuals are the model’s outer out-of-fold errors. Density, mean, and percentiles use this same Gaussian residual mixture. Its spread includes peer variation and finite-sample prediction error; it is not a pure estimate of irreducible variance.</p>`);
  html += section("Peer spread and percentile uncertainty", equation(bayesian || row.method === "gp" ? "Qp(θ) = exp(μθ + σθ Φ⁻¹(p))" : "Qp(b) = exp(μb + F⁻¹residual,b(p))") + `<p>The main curve is the predictive salary distribution; shaded ranges describe variation in another modeled peer’s pay. The small interval beside each percentile instead describes uncertainty about that distribution’s population percentile. Its level is selectable (89% by default). For Bayesian models we take equal-tailed intervals across posterior Qp draws. The GP uses its conditional normal posterior for the latent mean with fixed noise. ${!bayesian && row.method !== "gp" ? `For this model: ${escape(model.uncertainty.method)}. There are 256 uncertainty replicates; these are approximate compatibility intervals, conditional on the fitted design choices.` : ""}</p><p>The main displayed percentile comes from the averaged predictive distribution; it need not equal the median of parameter-specific percentiles. These operations answer different questions. None of these intervals captures uncertainty about whether the disclosure cohort represents RP’s hiring market.</p>`);
  html += section("Drivers and focus shares", `<p>Additive drivers compare each selected input with its reference: numeric training geometric means, zero centered category effects, and Functional overlap for EA. Kernel models allocate interactions using exact Shapley weights over all 2ᴾ numeric-input coalitions against the same zero-input reference. These are model associations, not causal effects. Displaying 100(exp(Δ) − 1) converts a log contrast to a multiplicative salary percentage; percentages do not add.</p><p>A focus mixture is a user-entered weighted sum of category effects in each posterior draw. It is a scenario assumption, not a fitted organizational composition. “Average category effect” uses the centered effect zero and is distinct from both observed category frequencies and RP staffing shares.</p>`);
  html += section("Validation and reproduction", `<p><strong>Organization-grouped 10-fold cross-validation</strong> uses the archived fixed fold assignments and seed ${artifact.provenance.seed}. Exact base-pay rows are the common scoring target. Error and CRPS are lower-is-better; log score is higher-is-better. A log-score difference Δ corresponds to a geometric mean predictive-density ratio exp(Δ), not percent accuracy. One split does not establish a definitive model ranking.</p><p>${bayesian ? "CmdStan uses four chains: initially 400 warmup + 500 saved iterations per CV chain; full fits and CV convergence refinements use 800 + 1,000. Target acceptance is 0.995/0.999 and maximum tree depth 13. Refined folds are recorded in the artifact; convergence thresholds are unchanged. The browser retains 512 evenly spaced posterior draws. Production builds check divergences, tree depth, R-hat, ESS, and energy diagnostics." : "The artifact records R/package versions, numeric coefficients or kernel factors, training row IDs, calibration residual IDs, and uncertainty replicates."}</p><p><a href="benchmark/analysis/predictive_salary_models/README.md" target="_blank">Reproduction guide</a> · <a href="benchmark/analysis/predictive_salary_models/fit_salary_models.R" target="_blank">Fit script</a> · <a href="benchmark/analysis/predictive_salary_models/ceo_salary_model.stan" target="_blank">Stan specification</a> · <a href="benchmark/analysis/predictive_salary_models/model_extensions.R" target="_blank">Spline and kernel implementation</a></p><p>References: <a href="https://mc-stan.org/docs/stan-users-guide/gaussian-processes.html" target="_blank" rel="noopener">Stan GP guide</a>; <a href="https://stat.ethz.ch/R-manual/R-devel/library/mgcv/html/gam.html" target="_blank" rel="noopener">mgcv documentation</a>; <a href="https://cran.r-project.org/web/packages/e1071/e1071.pdf" target="_blank" rel="noopener">e1071 documentation</a>; <a href="https://sites.stat.washington.edu/people/raftery/Research/PDF/Gneiting2007jasa.pdf" target="_blank" rel="noopener">Gneiting &amp; Raftery on proper scores</a>.</p>`);
  return html;
};
