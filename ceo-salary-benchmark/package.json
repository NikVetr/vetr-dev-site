{
  "name": "ceo-salary-benchmark",
  "private": true,
  "scripts": {
    "build:data": "python3 scripts/build_app_data.py",
    "build:routes": "python3 scripts/build_position_routes.py --site-root ..",
    "build": "npm run build:data && npm run build:routes",
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1 --directory ..",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
